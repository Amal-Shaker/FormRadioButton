import 'package:flutter/cupertino.dart';

class PushEx extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('hello world'),
    );
  }
}
